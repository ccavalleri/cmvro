/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['www.repstatic.it', 'avatarfiles.alphacoders.com','flowbyte.com'],
  },
};

module.exports = nextConfig;
